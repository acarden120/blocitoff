 <% if @item.destroyed? %>
   $('#item-' +<%= @item.id %>).hide();
   $('.js-items-count').html("<h3><%= pluralize(@user.items.count, 'item') %></h3>");
 <% else %>
   $('#item-' +<%= @item.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] %></div>");
 <% end %>